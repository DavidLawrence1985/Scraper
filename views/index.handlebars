<div class="wrapper">

    <div class="box-1">
        <h2>articles</h2>
            <ul class="list">
            {{#each articles }}
                
            <li>
                    <h1>{{title}}</h1>
                    <a href="{{link}}">View Story</a>
                    <p class="summary">{{summary}}</p>
                    <div id="notes"></div>

                   
            </li>      
                
            {{/each}}
            </ul>

            
        
    </div>