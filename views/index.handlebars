<div class="wrapper">

    <div class="box-1">
        <h1 id="main-title">articles</h2>
            <ul class="list">
            {{#each articles }}
                
            <li>
                    <h2 id="article-title">{{title}}</h1>
                    <a href="{{link}}" id="story">View Story</a>
                    <p class="summary" data-id={{_id}}>{{summary}}</p>
                    {{!-- <form id="note-form">
                        Name<br>
                        <input type="text" name="name" id="name-input" >
                        <br>
                        Note<br>
                        <input type="text-area" name="body" id="body-input" >
                        <br><br>
                        <input type="submit" value="Submit" id="save-note">
                        </form>  --}}
                    <p id="notes">{{Note}}</p>    
            </li>      
                
            {{/each}}
            </ul>

            
        
    </div>